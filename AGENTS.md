# Workspace Instructions

- For Unity work, limit searches, inspection, and routine changes to `Assets/_Project/Scripts` by default.
- Do not scan or inspect ScriptableObject, Prefab, Scene, serialized asset, or other non-script project directories unless the user explicitly requests it or names a specific file there.
